import React from "react";

const Cities = ({props}) => {

    return (
        <Icon
            raised
            name='search'
            type='font-awesome'
            color='#f50'
            onPress={() => console.log('hello')} 
        />
    )

}
export default Cities;